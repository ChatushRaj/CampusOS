## What this changes

<!-- One or two sentences on the effect, not the mechanics. -->

## Why

<!-- The problem this solves. Link an issue if there is one. -->

## How it was verified

- [ ] `npm run verify` passes
- [ ] Tested against a local database with `npm run seed`
- [ ] New rules that must always hold were added as database constraints, not just checks in code
- [ ] Authorisation was verified server-side, not only by hiding UI

## Screenshots

<!-- For anything visible. Before and after if it is a change rather than an addition. -->
